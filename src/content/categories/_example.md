---
# Use this file as template. 
# You can copy this file and rename it to create a new category.
#
# REMEMBER: Remove the `_` from the filename to make it a valid category.
# The filename should be in lowercase and hyphenated (e.g., `example-category.md`).
# The name should be in title case (e.g., "Example Category") as this will be visible on the website.
name: "Example Category"
---